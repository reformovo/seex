[package]
name = "seex"
version = "0.1.0-beta.0"
edition = "2024"
description = "Unified Rust SDK facade for Seex."
license = "MIT"
repository = "https://github.com/reformovo/seex"
publish = false

[dependencies]
seex-core = { path = "../seex-core" }
seex-model = { path = "../seex-model" }
seex-storage = { path = "../seex-storage" }

[lib]
name = "seex"
path = "src/lib.rs"
